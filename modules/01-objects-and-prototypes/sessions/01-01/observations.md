# Наблюдения 01-01

Заполните прогноз до первого запуска `lab.js`. Затем добавьте фактический вывод
и объясните только те строки, где наблюдение не совпало с прогнозом.

## Среда наблюдения

Команда: `node --version`

Фактическая версия:

## Прогноз

```text
initial timeout:
initial own timeout:
initial timeout in chain:
prototype is defaults:

shadowed timeout:
defaults timeout:
shadowed own timeout:

revealed timeout:
revealed own timeout:

own undefined value:
own undefined exists:
missing value:
missing exists:
```

## Фактический вывод

```text
Вставьте сюда результат собственного запуска.
```

## Что пришлось исправить в модели

Если прогноз совпал полностью, напишите, по каким объектам прошёл lookup в трёх
случаях: own property, inherited property и отсутствующий ключ.
